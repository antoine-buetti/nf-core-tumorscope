{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/cellsegmentation/master/assets/schema_input.json",
    "title": "nf-core/cellsegmentation pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "video": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.tif(f)?$",
                "errorMessage": "Video TIFF file must be provided, cannot contain spaces and must have extension '.tif' or '.tiff'"
            }
        },
        "required": ["sample", "video"]
    }
}
